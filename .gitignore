# Node + Vite + Java + IntelliJ + macOS
node_modules/
dist/
build/
out/
.idea/
.vscode/
target/

logs/
*.log
**/*.log
.DS_Store
.env
.env.*

# Uploads
uploads/
**/uploads/


# Python
.venv/

# Never track environment files or stray backend logs (public repo)
backend/.env.staging
# NOTE: frontend/.env.staging stays TRACKED — Vite reads it during `npm run build -- --mode staging`.
# It must contain public build-time values only (no secrets).
backend/backend_*.txt
claude_audit.txt
tmp/
# Local agent config (contains API keys)
.codex/
