spring:
  application:
    name: athleticaos-backend

  profiles:
    active: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true


server:
  port: 8080
  forward-headers-strategy: framework

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html

application:
  security:
    cors:
      allowed-origins: http://localhost:5173,http://localhost:3000
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000
      refresh-token:
        expiration: 604800000

athleticaos:
  security:
    identification-hmac-secret: ${ATHLETICAOS_IDENTIFICATION_HMAC_SECRET:}
    identification-hmac-version: ${ATHLETICAOS_IDENTIFICATION_HMAC_VERSION:1}
  backfill:
    identification:
      enabled: ${ATHLETICAOS_BACKFILL_IDENTIFICATION_ENABLED:false}
      dry-run: ${ATHLETICAOS_BACKFILL_IDENTIFICATION_DRY_RUN:true}
      batch-size: ${ATHLETICAOS_BACKFILL_IDENTIFICATION_BATCH_SIZE:100}
